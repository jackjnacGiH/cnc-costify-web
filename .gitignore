# Dependencies
node_modules/
__pycache__/
*.py[cod]
*.venv
.venv/
venv/
ENV/

# Build Output
build/
dist/
release_*/
*.exe
*.dmg
*.AppImage
*.pkg

# Environment variables & API keys — NEVER commit these
.env
.env.local
.env.*.local
api_config.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS generated files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Archives
*.rar
*.zip
*.7z
*.tar.gz

# Spec files (generated by pyinstaller, unless you want to keep custom ones)
# We are keeping custom spec files, so commenting this out or being specific
# *.spec

# --- Claude Code / Vercel local state ---
.claude/
.vercel/

# --- Next.js build output ---
.next/
out/

# --- Signing keys: NEVER commit private keys ---
*_private_key.pem

# --- Build / release artifacts ---
release/

# --- CAD sample files (STEP/STP) ---
*.STEP
*.step
*.stp
*.STP

# --- Local data dumps & scratch (kept on disk, not in git) ---
license_users.json
iges_*.json
pom21_*.json
*_iges_*.json
*_step_*.json
benchmark_*.csv
test_payload.json
tmp_profile.json
Backup*.json

# --- Scratch / archive dirs ---
_archive/
Test File/

# --- Local license artifacts ---
admin/*.dat

# --- Production provisioning and runtime data ---
# setup_phase_a.py historically embedded a live signing key. Keep it local only.
setup_phase_a.py
/website/data/
