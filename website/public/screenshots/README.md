# Screenshots for Hero / Home page

Drop these PNGs here to replace the styled mock fallbacks:

- `main-step.png`    — V5.1 main UI showing STEP volume + cost result panel
- `pdf-batch.png`    — PDF/JPG Costify with multi-file queue + summary table
- `license-tab.png`  — License tab with the green "🌐 บัญชีเว็บไซต์" card

**Recommended:** 1600×1000 px (16:10) PNG, < 500 KB each.
The frames use `object-cover object-top`, so the top of the image is what
shows in the card — line up the most interesting UI near the top.
