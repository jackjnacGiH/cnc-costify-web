# 💻 Electron + Offline License Key System (Manual Renewal) — Secure Edition

เอกสารฉบับนี้ปรับปรุงระบบสิทธิ์การใช้งานออฟไลน์สำหรับแอป Electron ให้ “ปลอดภัยขึ้น” โดยเน้นความถูกต้องของการตรวจสอบสิทธิ์ (integrity) ความทนต่อการแก้ไขปลอมแปลง (tamper-resistance) และประสบการณ์ใช้งานที่ชัดเจน โดยหลีกเลี่ยงการใช้คีย์ลับร่วมกันแบบฮาร์ดโค้ดและตัวอย่างคริปโตที่ไม่ปลอดภัย

สถาปัตยกรรมยึดหลัก “ลงลายเซ็นฝั่งผู้ดูแล – ตรวจสอบฝั่งลูกค้า” โดยไฟล์สิทธิ์การใช้งานเป็นข้อมูลแบบลงลายเซ็นดิจิทัล (digital signature) ด้วยคีย์ส่วนตัวของผู้ดูแล (Admin) และให้แอปลูกค้าตรวจสอบด้วยคีย์สาธารณะ (Public Key) ที่ฝังอยู่ในแอป ซึ่งไม่ใช่ความลับ

---

## 🎯 เป้าหมายและหลักการ
- Offline-first: ใช้งานและต่ออายุได้โดยไม่ต้องเชื่อมต่ออินเทอร์เน็ต
- Integrity มาก่อน Confidentiality: ยืนยันว่าไฟล์สิทธิ์การใช้งาน “ถูกต้องจากผู้ดูแล” และ “ไม่ถูกแก้ไข” เป็นอันดับแรก การเข้ารหัสเพื่อปกปิดเนื้อหาเป็นทางเลือกเสริม
- ไม่มีคีย์ลับฮาร์ดโค้ดในฝั่งลูกค้า: ฝังเฉพาะคีย์สาธารณะของผู้ดูแลได้ เพราะไม่ใช่ความลับ
- Crypto สมัยใหม่: ใช้ลายเซ็น Ed25519, การเข้ารหัสแบบ AES‑256‑GCM, KDF แบบ Argon2id/PBKDF2
- ตรวจสอบได้และหมุนคีย์ได้: รองรับการระบุเวอร์ชันอัลกอริทึม/คีย์ และรายการยกเลิก (revocation)
- เคารพความเป็นส่วนตัว: เก็บเฉพาะข้อมูลที่จำเป็นต่อการตรวจสอบสิทธิ์และฟีเจอร์

---

## 🛡️ โมเดลภัยคุกคาม (Threat Model)
- คัดลอก/แชร์ไฟล์สิทธิ์การใช้งานไปเครื่องอื่น
- แก้ไขไฟล์เพื่อยืดเวลาหมดอายุหรือเปิดฟีเจอร์
- ย้อนเวลาระบบเพื่อหลีกเลี่ยงวันหมดอายุ
- แกะ/ดัดแปลงโค้ดฝั่งลูกค้าหรือรีแพ็คตัวติดตั้ง (repack)
- Hook/Debug ขณะรันเพื่อลัดการตรวจสอบสิทธิ์การใช้งาน
- การรั่วไหลของคีย์ผู้ดูแล (admin private key)

การบรรเทา (Mitigations)
- ลงลายเซ็นไฟล์สิทธิ์การใช้งานด้วยคีย์ส่วนตัวผู้ดูแล และตรวจสอบด้วยคีย์สาธารณะในแอป
- ผูกสิทธิ์การใช้งานกับ `hardware_id` ที่ได้จากการผสมหลายตัวระบุ (เช่น MachineGuid/BIOS/Storage ID) แล้วแฮชให้เป็นค่าเดียว
- ใช้ `not_before`/`expires_at` พร้อม `grace_period_days` และ `time_tolerance_sec` เพื่อลดผลกระทบการปรับเวลา
- เก็บสำเนา `last_seen_expiry` และตรวจจับรูปแบบย้อนเวลาก้าวร้าว (เช่น เวลาระบบต่ำลงเกิน tolerance)
- Obfuscation/packaging เพื่อลดการแกะโค้ด แม้ไม่ใช่มาตรการความปลอดภัยหลัก
- จัดการคีย์ผู้ดูแลอย่างปลอดภัย, รองรับ `pubkey_id` เพื่อการหมุนคีย์และเพิกถอน

---

## 🧱 สถาปัตยกรรมระบบ
- Actors
  - Admin Tool: เครื่องมือของผู้ดูแลสำหรับออกไฟล์สิทธิ์การใช้งานและต่ออายุ
  - Client App: Electron + (Python server/Node) ที่อ่าน/ตรวจสอบไฟล์สิทธิ์การใช้งาน
  - License File: `license.dat` (รูปแบบ JSON ลงลายเซ็น)
  - Keystore: ที่เก็บคีย์ส่วนตัวฝั่งผู้ดูแล (อยู่นอกตัวแอปลูกค้า)
- Trust Boundary
  - ฝั่งลูกค้าเชื่อถือเฉพาะไฟล์สิทธิ์การใช้งานที่ “ลงลายเซ็นถูกต้อง” และตรงกับฮาร์ดแวร์เครื่องนั้น
  - คีย์ส่วนตัวผู้ดูแลไม่อยู่ในฝั่งลูกค้า
- Data Flow
  1) ลูกค้าส่ง `hardware_id` ให้ผู้ดูแล
  2) ผู้ดูแลออกไฟล์สิทธิ์การใช้งาน (ลงลายเซ็น) และส่งให้ลูกค้า
  3) แอปอ่านไฟล์ → ตรวจลายเซ็น/ช่วงเวลา/ฮาร์ดแวร์/ฟีเจอร์ → อนุญาตการใช้งาน

---

## 🔑 คีย์และคริปโต
- ลายเซ็นดิจิทัล: ใช้ `Ed25519` สำหรับลงลายเซ็น `license_payload` ด้วยคีย์ส่วนตัวผู้ดูแล
  - แอปฝังคีย์สาธารณะ (`pubkey`) พร้อม `pubkey_id` และ `sig_alg`
- การเข้ารหัสเนื้อหา (ทางเลือก): ใช้ `AES‑256‑GCM` หากต้องการปกปิดบางฟิลด์
  - KDF: `Argon2id` (แนะนำ) หรือ `PBKDF2‑HMAC‑SHA256` พร้อม `salt` และพารามิเตอร์รัดกุม
  - หมายเหตุ: ในระบบออฟไลน์ การปกปิดเนื้อหาไม่แข็งแรงเท่าการยืนยันความถูกต้อง จึงให้ความสำคัญกับลายเซ็น

---

## 📄 รูปแบบไฟล์สิทธิ์การใช้งาน (`license.dat`)
ไฟล์เป็น JSON ลงลายเซ็น โดยลงลายเซ็นเหนือ `payload` ทั้งก้อน:

```json
{
  "version": 1,
  "payload": {
    "license_id": "LIC-2025-000123",
    "product": "CNCcostifyAI",
    "customer_name": "ACME Co.",
    "hardware_id": "sha256:8cfa...",
    "issued_at": "2025-10-10T09:00:00Z",
    "not_before": "2025-10-10T00:00:00Z",
    "expires_at": "2026-10-10T00:00:00Z",
    "grace_period_days": 7,
    "time_tolerance_sec": 300,
    "features": { "pro": true, "module_x": true },
    "max_version": "1.2.x",
    "build_channel": "stable",
    "revoked": false
  },
  "signature": "base64:MEUCIQC...",
  "sig_alg": "Ed25519",
  "pubkey_id": "admin-key-2025",
  "enc": {
    "enabled": false,
    "enc_alg": "AES-256-GCM",
    "salt": "base64:...",
    "ciphertext": null
  }
}
```

หมายเหตุ
- หาก `enc.enabled = true` ให้เข้ารหัสเฉพาะบางฟิลด์ใน `payload` ด้วย AES‑GCM แต่ยังคง “ลงลายเซ็นเหนือข้อมูลหลังถอดรหัส” เพื่อคงความถูกต้อง
- `hardware_id` ควรเป็นแฮชรวมของหลายตัวระบุที่เสถียร เพื่อบรรเทาการโคลนเครื่อง

---

## 🧩 การผูกฮาร์ดแวร์ (Hardware Binding)
- แนะนำรวมหลายแหล่ง เช่น `MachineGuid` (`HKLM\SOFTWARE\Microsoft\Cryptography\MachineGuid`), BIOS serial, disk/volume ID แล้วทำแฮชรวมเป็น `hardware_id`
- รองรับ fallback/override กรณีเครื่องเปลี่ยนอะไหล่ (ผู้ดูแลออกสิทธิ์การใช้งานใหม่โดยยืนยันตัวตนลูกค้า)
- อธิบายล่วงหน้าว่าการเปลี่ยนเมนบอร์ด/ดิสก์อาจกระทบสิทธิ์การใช้งาน เพื่อความโปร่งใส

---

## ⏱️ การป้องกันย้อนเวลา
- ใช้ `not_before`/`expires_at` + `grace_period_days` และ `time_tolerance_sec`
- เก็บ `last_seen_expiry` และ `last_validation_time` ในพื้นที่ผู้ใช้ แล้วตรวจจับ pattern เวลาถอยหลังมากผิดปกติ
- หลีกเลี่ยงพึ่ง NTP ออนไลน์ แต่รองรับการตรวจเพิ่มหากมีการเชื่อมต่อในอนาคต

---

## 📦 การจัดเก็บไฟล์และสิทธิ์
- ตำแหน่งไฟล์สิทธิ์การใช้งาน
  - ต่อเครื่อง: `\ProgramData\CNCcostifyAI\license.dat`
  - ต่อผู้ใช้: `\Users\<User>\AppData\Roaming\CNCcostifyAI\license.dat`
- สิทธิ์ (ACL)
  - ไฟล์ควรอ่านได้โดยผู้ใช้ที่รันแอป และเขียนได้เฉพาะแอป/แอดมิน
  - หลีกเลี่ยงตำแหน่งที่ world-writable
- ล็อก/บันทึกเหตุการณ์
  - `\ProgramData\CNCcostifyAI\logs\server.log` ระบุเหตุผลที่สิทธิ์การใช้งานไม่ผ่านเพื่อช่วยซัพพอร์ต

---

## 🔁 เวิร์กโฟลว์ใช้งาน/ต่ออายุ (Offline)
- Activation
  1) แอปแสดง `hardware_id` ให้ลูกค้า
  2) ลูกค้าส่งให้ผู้ดูแล
  3) ผู้ดูแลออกไฟล์ `license.dat` ที่ลงลายเซ็นแล้ว ส่งกลับให้ลูกค้า
  4) ลูกค้าวางไฟล์ตามตำแหน่งที่กำหนด → แอปตรวจสอบและเปิดใช้
- Manual Renewal
  1) ก่อนหมดอายุ/หลังหมดอายุในช่วงผ่อนผัน ลูกค้าแจ้งขอต่ออายุ
  2) ผู้ดูแลออกไฟล์ใหม่ที่มี `expires_at` ใหม่ (ลงลายเซ็น)
  3) ลูกค้าแทนที่ไฟล์เดิม

- UX ข้อเสนอ
- หน้า “สถานะสิทธิ์การใช้งาน” ระบุวันหมดอายุ/ช่วงผ่อนผัน/ฮาร์ดแวร์ที่ผูกไว้
- ปุ่ม “นำเข้าไฟล์สิทธิ์การใช้งาน” และข้อความผิดพลาดที่ชัดเจน
- ไม่เปิดเบราว์เซอร์อัตโนมัติเมื่อเริ่มโปรแกรม

---

## 🗝️ การจัดการคีย์ผู้ดูแล (Admin Key Management)
- เก็บคีย์ส่วนตัวในเครื่องมือ Admin ที่ป้องกันการเข้าถึง (เช่น HSM/Password manager/เครื่องออฟไลน์)
- ระบุ `pubkey_id` ในไฟล์สิทธิ์การใช้งานเพื่อรองรับการหมุนคีย์
- รายการเพิกถอน (`revocation list`) ตาม `license_id` หรือชุด `pubkey_id`
- แบ็กอัพและนโยบายการเข้าถึงโดยผู้มีสิทธิ์เท่านั้น

---

## ✅ การทดสอบและตรวจสอบ
- Unit tests: ตรวจสอบการยืนยันลายเซ็น, การตีความช่วงเวลา, การจับเวลาไม่ปกติ
- Integration tests: อ่าน/เขียนไฟล์ในตำแหน่งที่กำหนด, สิทธิ์ไฟล์
- Tamper tests: เปลี่ยนค่าในไฟล์ → แอปต้องปฏิเสธ
- Time rollback tests: ย้อนเวลาระบบภายใน tolerance → เตือน, เกิน tolerance → ปฏิเสธ

---

## 🧹 สิ่งที่ตัดออก/ลดทอน
- ตัวอย่างเข้ารหัสที่ใช้ `AES-ECB`, `crypto.createCipher/Decipher` แบบ passphrase, หรือโค้ดที่แชร์ `SECRET_KEY` ระหว่างฝั่งลูกค้า/ผู้ดูแล
- การฝังคีย์ลับฮาร์ดโค้ดในแอปลูกค้า
- การต่ออายุด้วย “รหัสลับ” ที่ถอดรหัสในฝั่งลูกค้าโดยตรง → เปลี่ยนเป็น “ไฟล์สิทธิ์การใช้งานลงลายเซ็น” ที่ผู้ดูแลออกให้

---

## 🧭 Roadmap แนะนำ
- ใช้ `Argon2id` สำหรับ KDF เมื่อพร้อม
- เพิ่มระบบ `revocation list` ฝั่งลูกค้า (ไฟล์ JSON จากผู้ดูแล) เป็นทางเลือก
- สำรองการตรวจเวลาผ่าน monotonic clock และ heuristic เพิ่มเติม
- ปรับปรุงเครื่องมือ Admin ให้ใช้งานง่าย พร้อมบันทึกการออกสิทธิ์การใช้งาน

---

## 📗 สรุปแนวทาง
- ใช้สถาปัตยกรรม “ลงลายเซ็นฝั่งผู้ดูแล – ตรวจสอบฝั่งลูกค้า” เป็นหลัก
- ให้ความสำคัญกับความถูกต้อง/ความทนต่อการแก้ไข มากกว่าการปกปิดเนื้อหา
- กำหนดรูปแบบไฟล์สิทธิ์การใช้งานที่ชัดเจน, ผูกกับฮาร์ดแวร์, มีช่วงเวลาและ tolerance ที่เหมาะสม
- ยกเลิกการใช้คีย์ลับฮาร์ดโค้ดและตัวอย่างคริปโตที่ไม่ปลอดภัย

เมื่อคุณยืนยันแนวทางฉบับนี้ ผมจะดำเนินการปรับโค้ดแอปและเครื่องมือ Admin ให้สอดคล้อง โดยยังคงเวิร์ชันแอปตามที่กำหนด และรองรับการแพ็กตัวติดตั้งใหม่อย่างต่อเนื่อง

---

## 🔒 บันทึกการอัปเดต Dependencies และความปลอดภัย (2025‑10‑11)
- อัปเดต `electron` (devDependency) จาก `^31.0.0` เป็น `^35.7.5`
  - เหตุผล: แก้ไขช่องโหว่ ASAR Integrity Bypass (GitHub Advisory: `GHSA-vmqv-hx8q-j7mg`).
- อัปเดตชุด `electron-builder` ภายใต้ช่วง `^24.x` (effective version ระหว่างแพ็คคือ `24.13.3`)
  - เหตุผล: ปิดช่องโหว่ฝั่ง installer/packaging ที่ถูกแจ้งเตือนโดย `npm audit` (app-builder-lib/dmg-builder chain).
- ผลลัพธ์จากความปลอดภัย: รัน `npm audit` หลังแก้ไขแล้วได้ผล `found 0 vulnerabilities`.
- ตรวจสอบการแพ็ค: รัน `npm run dist:win` สำเร็จ → ไฟล์ติดตั้งอยู่ที่ `release\CNCcostifyAI Setup-1.2.9.exe` และมี blockmap ตามค่าเริ่มต้นของ NSIS.

คำสั่งที่ใช้
```powershell
npm audit
npm audit fix
# ปรับ devDependencies.electron เป็น ^35.7.5
npm install
npm run dist:win
```

หมายเหตุการดูแลรักษา
- แนะนำตรึงเวอร์ชันแบบไม่ใช้ตัวปะ (`^`) สำหรับสภาพ production ที่ต้องการความเสถียรสูง
- เมื่อต้องอัปเดตความปลอดภัย ให้ทดสอบแพ็ค/ติดตั้ง และตรวจหน้า Activation (สวิตช์ภาษา/เลือกไฟล์) ทุกครั้ง

---

## 📦 หมายเหตุสำหรับการติดตั้งลูกค้า — เวอร์ชันล่าสุด
- เวอร์ชันตัวติดตั้งล่าสุด: `1.2.9` (วันที่ 2025‑10‑11)
- ตำแหน่งไฟล์: `release\\CNCcostifyAI Setup-1.2.9.exe`
- เช็กซัม SHA256: `4809247A0504773B26140E174F98C0F291120A902D7BF6A9D07C497A1040E1F5`

ขั้นตอนติดตั้งแบบมาตรฐาน (Windows)
- ดับเบิลคลิกไฟล์ติดตั้ง → รูปแบบ `NSIS` แบบ one‑click (ไม่ต้องเลือกไดเรกทอรี)
- โหมดติดตั้งค่าเริ่มต้น: per‑user (`perMachine=false`) ตำแหน่งโดยปกติ: `%LocalAppData%\Programs\CNC Costify AI`
- เปิดแอปแล้วไปที่หน้า Activation เพื่อนำเข้าไฟล์สิทธิ์การใช้งานและเลือกตำแหน่งจัดเก็บ (Roaming/ProgramData/Dev)

การตรวจสอบความถูกต้องของไฟล์
- รันคำสั่ง PowerShell: `Get-FileHash -Algorithm SHA256 "release\\CNCcostifyAI Setup-1.2.9.exe"`
- เทียบค่าผลลัพธ์กับเช็กซัมที่ระบุด้านบนเพื่อยืนยันว่าไฟล์ไม่ถูกแก้ไข

หมายเหตุ
- หาก Windows SmartScreen เตือน ให้เลือก “Run anyway” เมื่อยืนยันว่าที่มาของไฟล์ถูกต้อง
- แนะนำให้ผู้ดูแลเผยแพร่เช็กซัมในช่องทางที่ลูกค้าสามารถตรวจสอบได้ (เช่น เอกสารหรืออีเมล)